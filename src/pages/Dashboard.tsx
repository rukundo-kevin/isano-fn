import Sidebar from "../_layouts/Sidebar";

const handleToggle = () => {};
const Dashboard = () => {
  return (
    <>
      <Sidebar style="" toggle={handleToggle} />
    </>
  );
};

export default Dashboard;
