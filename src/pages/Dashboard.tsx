import Sidebar from "../components/Sidebar";

const handleToggle = () => {};
const Dashboard = () => {
  return (
    <>
      <Sidebar style="" toggle={handleToggle} />
    </>
  );
};

export default Dashboard;
